using System;

namespace C01_Coverlet
{
    public class Class1
    {
        public int Add(int x, int y)
        {
            if (x > 0)
                return x + y;
            else
                return 0;
        }

        public int Divide(int x, int y)
        {
            return x / y;
        }
    }
}
